upstream web {
  server backend:8000;
}

server {
    listen 8002;
    server_name localhost;

    location /static/ {
        autoindex on;
        alias /static/;
    }
    location /media/ {
        alias /django_project/media/;
    }

    location / {
        proxy_pass http://web/;
    }
}